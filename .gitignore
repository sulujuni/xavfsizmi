__pycache__/

# Environment
.env
.venv/
.venv-test/
venv/

# SQLite database (Phase 2) — runtime data, not source
safelink.db
safelink.db-wal
safelink.db-shm
*.db-wal
*.db-shm

# Legacy / runtime data files
rate_tracker.json
data/
dump.rdb

# Logs. httpx logs every Telegram request at INFO, and the bot token is part of
# the request URL — these files contain live credentials and must never be
# committed.
*.log

