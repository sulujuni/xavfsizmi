# Core
python-telegram-bot==21.3
aiohttp==3.9.1
httpx>=0.25.0

# Database (Phase 2)
aiosqlite>=0.19.0

# Caching (Phase 2 - optional, falls back to memory if not installed)
redis>=5.0.0

# Image processing
opencv-python-headless==4.8.1.78
pillow==11.0.0
numpy==1.26.4

# Scheduling
apscheduler==3.10.4

# Utilities
python-whois==0.8.0
python-dotenv==1.0.0
